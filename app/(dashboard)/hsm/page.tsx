import React from 'react';
import DataTable from './data-table';

type Props = {};

const HSMPage = (props: Props) => {
  return (
    <div>
      <DataTable />
    </div>
  );
};

export default HSMPage;
